
class Person:
    # attribute (data)
    def __init__(self, name, age):
        self.name = name
        self.age = age

    # method (function)
    def greet(self):
        print(f"Hello, my name is {self.name} and I’m {self.age} years old.")class Person:
    # attribute (data)
    def __init__(self, name, age):
        self.name = name
        self.age = age

    # method (function)
    def greet(self):
        print(f"Hello, my name is {self.name} and I’m {self.age} years old.")
